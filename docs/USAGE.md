TODO: complete documentation

CLONE command (same as GUI command):

```bash
<packageManager> freshland@latest clone ProxityStudios/freshland ./freshland-fork
<packageManager> freshland@latest clone ProxityStudios/freshland#<commitHash> ./freshland-fork
<packageManager> freshland@latest clone ProxityStudios/freshland#<branch> ./freshland-fork
<packageManager> freshland@latest clone ProxityStudios/freshland#<tag> ./freshland-fork
<packageManager> freshland@latest clone ProxityStudios/freshland/<subFolder> ./freshland-fork/sub-folder
<packageManager> freshland@latest clone ProxityStudios/freshland/<subFolder>#<branch or CommitHash or tag> ./freshland-fork/sub-folder

<packageManager> freshland@latest clone https://gitlab.com/ProxityStudios/freshland ./freshland-fork
<packageManager> freshland@latest clone https://gitlab.com/ProxityStudios/freshland#<commitHash> ./freshland-fork
<packageManager> freshland@latest clone https://gitlab.com/ProxityStudios/freshland#<branch or CommitHash or tag> ./freshland-fork

<packageManager> freshland@latest clone gitlab@ProxityStudios/freshland#<branch or CommitHash or tag> ./freshland-fork
<packageManager> freshland@latest clone bitbucket@ProxityStudios/freshland#<branch or CommitHash or tag> ./freshland-fork
<packageManager> freshland@latest clone github@ProxityStudios/freshland#<branch or CommitHash or tag> ./freshland-fork
<packageManager> freshland@latest clone git@ProxityStudios/freshland#<branch or CommitHash or tag> ./freshland-fork

<packageManager> freshland@latest clone gitlab@ProxityStudios/freshland#<branch or CommitHash or tag> ./freshland-fork
<packageManager> freshland@latest clone bitbucket@ProxityStudios/freshland#<branch or CommitHash or tag> ./freshland-fork
```